import React from 'react';
// Next.js app 디렉토리에서는 기본적으로 파라미터 없이 페이지를 export해야 라우팅이 정상 동작합니다.
export default function StoreDashboard() {
  // 지점 대시보드: 개별 실적, 정책 수신 등
  return <div>지점 대시보드 (지점별 현황)</div>;
} 