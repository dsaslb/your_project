# 환경설정 예시 (PostgreSQL)

```
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=your_db
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password
```

- 위 환경변수를 .env 파일에 추가하세요.
- 운영 환경에서는 반드시 강력한 비밀번호와 별도의 DB 계정을 사용하세요. 